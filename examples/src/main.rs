use khaluda::khaluda;

khaluda! {
    שמש בית::אוספים::מילון;

    פעולה ראשית() {
        שיהיה משתנה מיל = מילון::חדש();
        מיל.להוסיף(2, 4);
        הדפס_שורה!("{מיל:?}");
        מיל.להוסיף(3, 6);
        הדפס_שורה!("{מיל:?}");
    }
}
